export interface LoginData {
  email: string;
  password: string;
}

export interface userData {
  nick: string;
  name: string;
  userId: string;
}

export interface randomNumberData {
  number: number;
}
